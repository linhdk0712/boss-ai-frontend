# API Configuration
# Backend API base URL
VITE_API_BASE_URL=http://localhost:8080/api/v1

# WebSocket Configuration
# WebSocket base URL for real-time updates
VITE_WS_BASE_URL=ws://localhost:8080

# Development Notes:
# - Copy this file to .env.local for local development
# - Set VITE_API_BASE_URL to your production API URL in Vercel environment variables
# - Set VITE_WS_BASE_URL to your production WebSocket URL in Vercel environment variables
# - All variables must be prefixed with VITE_ to be exposed to the client
