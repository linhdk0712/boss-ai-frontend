# Production Environment Configuration
# This file is used for production builds

# API Configuration
# Backend API base URL (HTTPS for production)
VITE_API_BASE_URL=https://bossai.com.vn/api/v1

# WebSocket Configuration
# WebSocket base URL for real-time updates (WSS for secure WebSocket)
VITE_WS_BASE_URL=wss://bossai.com.vn

# Production Notes:
# - Use HTTPS for API calls (https://)
# - Use WSS for WebSocket connections (wss://)
# - These variables are baked into the build at compile time
# - For Vercel deployment, also set these in Vercel environment variables
